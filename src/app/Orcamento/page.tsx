import { Form, Header } from "@/components";

const Orcamento = () => {
  return (
    <div className="pedido-container">
      <Header typeOrder={1} />
      <Form />
    </div>
  );
};

export default Orcamento;